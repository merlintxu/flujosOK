diff --git a/app/Infrastructure/Http/RingoverClient.php b/app/Infrastructure/Http/RingoverClient.php
index 1234567..89abcde 100644
--- a/app/Infrastructure/Http/RingoverClient.php
+++ b/app/Infrastructure/Http/RingoverClient.php
@@
-namespace FlujosDimension\Infrastructure\Http;
-
-use GuzzleHttp\Client;
-
-class RingoverClient
-{
-    private Client $http;
-
-    public function __construct(Client $http)
-    {
-        $this->http = $http;
-    }
+namespace FlujosDimension\Infrastructure\Http;
+
+use GuzzleHttp\Client as GuzzleClient;
+
+class RingoverClient
+{
+    /**
+     * Puede ser un cliente Guzzle puro o el wrapper HttpClient interno.
+     * @var GuzzleClient|HttpClient
+     */
+    private $http;
+
+    /**
+     * @param GuzzleClient|HttpClient $http
+     */
+    public function __construct($http)
+    {
+        $this->http = $http;
+    }
@@
-    public function get(string $url, array $options = []): array
-    {
-        $response = $this->http->get($url, $options);
-        return json_decode($response->getBody()->getContents(), true);
-    }
+    public function get(string $url, array $options = []): array
+    {
+        if ($this->http instanceof HttpClient) {
+            // Usa el wrapper interno
+            $response = $this->http->get($url, $options);
+        } else {
+            // Asume GuzzleClient
+            $response = $this->http->get($url, $options);
+        }
+        return json_decode((string) $response->getBody(), true);
+    }
